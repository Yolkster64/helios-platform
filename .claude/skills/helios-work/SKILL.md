---
name: helios-work
description: Use the canonical HELIOS work skill for shared task packets, bounded agent assignments, isolated workspaces, provider routing, and evidence handoffs across clients.
---

# HELIOS work entrypoint

Read `plugins/helios-connect/skills/helios-work/SKILL.md` from this reviewed
repository's root, then follow it for the requested task. That file contains
the shared workflow; this entrypoint intentionally keeps no second copy.
Read the current `AGENTS.md` for repository authority. If the canonical file
is unavailable, report that missing source and request it through the configured
HELIOS MCP connection; do not silently substitute a different skill revision.
