# scripts/github: repository truth, applied from `main`

The scripts that reconcile `Yolkster64/helios-platform`'s live GitHub configuration with
what is checked in, plus the two hygiene passes. They are driven by
`.github/workflows/governance-apply.yml` (rulesets, settings, labels, milestones) and
`.github/workflows/branch-prune.yml` (branch prune); the duplicate-issue closer runs from an
owner login. Design and the one owner step (the `HELIOS_ADMIN_TOKEN` fine-grained PAT, with
the click path): `docs/architecture/CONNECTIONS_SETUP.md` § Control fabric.

## Shared contract

- **Dry run by default.** Without `-Apply` a script only GETs, diffs and prints; `-Apply` is
  the only thing that mutates.
- **GET-diff-then-print-then-apply, per item.** The exact `gh api` command is printed
  BEFORE it runs, so an interrupted pass still leaves the replay line; a failed item never
  aborts the pass, and the run ends with a consolidated replay list.
- **Exit codes**: `0` = success or clean dry run; `1` = an item failed (replay list printed)
  or the input is invalid; `2` = precondition or credential missing (`gh` not on PATH, the
  repository unreadable on the wire, or the required permission absent under `-Apply`).
- **Wire truth, never `gh auth status`**: the precondition is `gh api repos/{repo}`'s exit
  code plus one boolean `.permissions.*` field (`scripts/verify/rest-connect.ps1` doctrine).
- **`-Json`** emits exactly one object on stdout and nothing else (human lines suppressed or
  moved to stderr); the exit contract is unchanged.
- **Secrets**: no token value is ever read, stored or printed; `gh` reads `GH_TOKEN` by name
  or its keyring, and the scripts see only exit codes and JSON.
- **One wire layer, copied verbatim into each script** (`$script:GhApiArgs` +
  `Invoke-GhApi`): every `gh api` call carries `X-GitHub-Api-Version: 2022-11-28` (gh 2.63.2
  sends none by itself) and the printed replay lines show it; a 429, or a 403 whose message
  names the rate/secondary limit, sleeps `Retry-After` (60 s when absent) and retries ONCE;
  no other status is retried (a 4xx fails identically, a blind POST retry double-creates);
  mutations are paced >= 1 s apart (prune `-Apply` issues ~190 against the ~80/min
  secondary limit). Lists page explicitly with `per_page=100&page=N` because
  `gh api --paginate` follows `Link` to `repositories/<id>/…`, which the agent proxy 403s.
- **Existence reads are three-way**: only GitHub's own `"status":"404"` body means "absent"
  (plan the create); any other failed read is `unknown (HTTP n / reason)` - no mutation,
  informational in a dry run, a failed item under `-Apply` (a blind POST would 409).
- **Measured from agent containers** (2026-09-05): the proxy there returns
  `.permissions.admin` and `.permissions.push` as `false`, blocks `repos/{repo}/pages` and
  the `actions/secrets|variables` listings (HTTP 403, no `status` field in the body) and
  rejects `%23` in paths (HTTP 400 "could not be canonicalized"), so only the dry runs are
  possible; the apply path is the workflow on `main` or an owner workstation.
- **Attribution-footer caveat**: label and milestone descriptions written through the Claude
  container's proxy come back with a trailing `_Generated by [Claude Code](https://claude.ai/code)_`
  footer (measured on all 4 milestones). The manifests carry the plain text, so the
  workflow's first run on `main` reconciles them as ordinary description PATCHes; until then
  the milestone dry run reads `update=4`.

## The scripts

| Script | What it reconciles | Dry run | `-Apply` needs | Exit |
| --- | --- | --- | --- | --- |
| `apply-rulesets.ps1` | `.github/rulesets/*.json` → repository rulesets; matched by NAME among the repo's OWN rulesets (`source_type` Repository - `GET /rulesets` lists org rulesets too). A match is read back (`GET /rulesets/{id}`) and compared on enforcement / target / conditions / rules / bypass_actors (keys sorted, arrays order-insensitive, nulls and server ids/timestamps ignored) → `in sync` and no call, else PUT in place; no match → POST. Header = runbook for the required checks in `main.json` (cited by job, not line), why each excluded check is excluded, and the ORDERING CONSTRAINT: apply only after PR #113's no-path-filter `pull_request` triggers are on `main`, or 4 of 8 contexts strand as "Expected" with nobody able to bypass (`bypass_actors: []`). `-Repository`, `-RulesetDirectory`. | Prints the POST/PUT calls or `in sync`; measured: 0 rulesets live, 1 to create | repo admin (`.permissions.admin`) | 0 / 1 (invalid file or API failure) / 2 |
| `apply-repo-settings.ps1` | Scalar settings: `pages` (`build_type=workflow`; only a `"status":"404"` body plans the POST, any other failed read is `unknown (HTTP n / reason)`), `auto-merge`, `wiki`, `delete-branch-on-merge`, `label:automerge` (name/color/description read from `config/github/labels.json`, the manifest `apply-labels.ps1` owns; literal fallback only when that entry is absent; existence only, drift is the labels script's job); `boards` is REPORT-ONLY (Projects v2 needs a classic `project`-scope PAT, commands printed). `-Repository`. | Live-vs-desired per item; measured: auto-merge, wiki and the automerge label in sync, delete-branch-on-merge off (1 PATCH shown), pages `unknown (HTTP 403 / …not permitted through this proxy)` → informational, exit 0 | repo admin | 0 / 1 (replay list; an unknown read counts) / 2 |
| `apply-environments.ps1` | `config/github/environments.json` → deployment environments: `wait_timer`, `prevent_self_review`, the reviewer set (logins and team slugs resolved to ids first) and the deployment branch policy, compared against the live environment before anything is written. The allowed branch PATTERNS come from their own endpoint, because a custom policy allowing `feature/*` has the same shape in the environment body as one allowing only `main`: a pattern the manifest wants and the repository lacks is POSTed under `-Apply`; one the repository allows and the manifest does not is an owner action, since removing it is a DELETE this script never makes. A reviewer that cannot be resolved BLOCKS that environment (GitHub's PUT replaces the list wholesale, so a partial write would silently remove the reviewers it could not resolve) → exit 1, a fault in the manifest rather than in the credential. `secretNames` / `variableNames` are reported as NAMES with the exact `gh secret set --env` command; no value is ever read. `-Repository`, `-ManifestPath`, `-Json`. | Live-vs-desired per environment; measured: `production` **absent**, so the PUT and the `main` branch-pattern POST are both printed, and the workflow naming it would create it UNPROTECTED | repo admin | 0 (incl. a dry run with pending changes) / 1 (unresolvable reviewer, or an item failed) / 2 (precondition only) |
| `verify-environment-gate.ps1` | **Not a reconciler — a gate.** Reads one environment and exits 0 only when a `required_reviewers` rule with at least one reviewer exists, so `.github/workflows/helios-deploy.yml` cannot reach the tenant through an environment that gates nothing. Runs as the `verify-gate` job in front of the deploy job, in a job that deliberately does NOT name the environment (a verifier that named it would create the thing it checks for, then wait for the approval it exists to prove is required). Fails CLOSED, which is the opposite posture from the four reconcilers above: absent, no protection rules, an emptied reviewer list, a 403, a 5xx and a missing `gh` all refuse, because "I could not check" is not a reason to deploy. A `wait_timer` alone is refused with that said out loud - it delays the deployment and then runs it unattended. `-Repository`, `-EnvironmentName`, `-Json`. | Reads only; makes no call that can change anything | any credential that can read the environment (the workflow `GITHUB_TOKEN` does) | 0 (a reviewer is required) / 1 (everything else, including "could not tell") - there is deliberately no 2 |
| `apply-labels.ps1` | `config/github/labels.json` (21 labels) → live labels: POST missing, PATCH drifted color/description on the live name; never deletes, never renames; unmanaged live labels listed. An entry carrying any key outside `name`/`color`/`description` (a `descr` typo, `aliases`) is `invalid` → exit 1 with the key named. `-Repository`, `-ManifestPath`, `-Json`. | Per label create/update/in-sync; measured: create 0, update 0, in sync 21 | `issues: write` (Actions `GITHUB_TOKEN` or a PAT with Issues RW); precondition is only that the repo is readable | 0 / 1 (manifest invalid or item failed) / 2 |
| `apply-milestones.ps1` | `config/github/milestones.json` (4 milestones with due dates) → live milestones by TITLE: POST missing, PATCH description/`due_on` on open ones; closed matches left untouched; never closes, reopens or deletes. `due_on` sent as noon UTC and compared by UTC calendar date so re-runs read in sync. Any key outside `title`/`description`/`due_on` (`dueOn`, `state`) is `invalid` → exit 1 with the key named. `-Repository`, `-ManifestPath`, `-Json`. | Per milestone; measured: 4 live, `update=4` (the attribution footer on their descriptions, see above), in sync once the workflow has run | `issues: write` | 0 / 1 / 2 |
| `prune-branches.ps1` | Every remote branch, classified from live reads: `open-pr` (head of an open PR here or in the upstream parent of a fork) keep, `keep-list` (default, `main`, `develop`, `master`, `-Keep`) keep, `merged` (`compare/{default}...{tip sha}` says `ahead_by == 0` - the sha, never the name, goes in the URL) delete, `stale` tag `archive/<branch>` at the tip then delete (tip re-read first; a tag at another sha fails the item), `unclassified` skip → exit 1. Only the `-Apply` tip re-read and the DELETE address the ref by percent-encoded name. `-MaxDelete` (200) refuses a larger apply with exit 2; a failed parent-PR read is exit 2 too. `-Repository`, `-Json` (adds `parent` and `nextStep`), `-Keep`, `-ArchivePrefix`. | Full table + every POST/DELETE; measured: 105 branches, 4 open-pr (3 local, 1 upstream), 3 keep-list, 10 merged, 88 stale, 0 unclassified (the `…-#85` branch classifies by sha) → exit 0 here | `contents: write` (`.permissions.push`); intended path `gh workflow run branch-prune.yml -f apply=true -f keep=<a,b>` | 0 / 1 / 2 |
| `close-duplicate-issues.ps1` | Issues `-From` 54 `-To` 92: the `[GH-<n>] …` twins Linear's GitHub integration re-imported. The loop marker is built from `config/connectors.json` `linear.titlePrefix` (the key `linear-sync.yml` reads; escaped, `{number}` → `(\d+)`; built-in `^\[GH-(\d+)\]` only when the file/key is absent, and the "loop marker" line says which). Loop duplicate only when open + title marker + body `GitHub (system of record)` line + original `#n` open; then `duplicate` label, ONE comment with the footer (every comment page read, `per_page=100&page=N`), `PATCH state=closed state_reason=duplicate duplicate_issue_id=<original .id>` (each step idempotent). OWNER STEP FIRST: Linear → Settings → Integrations → GitHub → team John → issue sync OFF, or they reopen. `-Repository`, `-Json`, `-From`/`-To`, `-SessionUrl` (optional session link under the footer; any other shape = exit 1), `-ConnectorsPath`. | Table of number / action / original; measured: 36 candidates, 3 skipped (#77, #85, #87: originals closed), 0 unreadable; with a scratch `[LIN-{number}]` prefix (trailing space included): 0 candidates, 39 `skip:not-loop-title` | push (`.permissions.push`): an owner login or the PAT; needs reads even for the dry run (exit 2 without them) | 0 / 1 (invalid range or a mutation failed) / 2 |
| `GitHubIntegration.psm1` | Legacy metrics/board simulation module (upstream `M0nado` defaults). Not a shared home for the scripts above and not part of the control fabric. | n/a | n/a | n/a |

**Before a ruleset is applied**, `python3 scripts/validation/validate_ruleset_contexts.py`
answers the one question that can brick this repository: does every context
`.github/rulesets/*.json` requires actually REPORT on every pull request it gates? With
`bypass_actors: []` a context that never reports does not fail a pull request, it strands it -
unmergeable by anyone, including the owner, until an administrator edits the ruleset. The
checker fails on a context no job produces, a workflow with no `pull_request` trigger, a
trigger carrying `paths` / `paths-ignore` (absent is not the same as skipped), and a trigger
whose `branches` miss the branch the ruleset gates. It runs in `quality.yml`'s Automation
Wiring Validation job, which feeds the required `Quality Check Summary` context, and its own
mutation tests (`scripts/validation/tests/test_validate_ruleset_contexts.py`) run beside it.

Restore a pruned stale branch from its archive tag:

```bash
git push origin refs/tags/archive/<branch>:refs/heads/<branch>
```

## How the workflows drive them

- **`governance-apply.yml`** — `plan` job on `pull_request` touching `.github/rulesets/**`,
  `config/github/**`, `scripts/github/**` or the workflow (`GITHUB_TOKEN`, `contents: read` +
  `pages: read`, all five scripts in dry run, diff table in the job summary); `apply` job
  on `push` to `main` (same paths), the daily `schedule` (06:17 UTC drift net) or
  `workflow_dispatch` (`apply` boolean, `scope`
  `all|rulesets|settings|environments|labels|milestones`) with `GH_TOKEN` = the HELIOS
  GitHub App installation token when the runner could mint one, else `HELIOS_ADMIN_TOKEN`
  when stored, else `github.token`. Labels and milestones apply with any of them; rulesets,
  settings and environments apply only with one of the two real admin credentials and
  otherwise print their dry run under a "needs an admin credential" row. Exit 2
  rows are reported, never red; exit 1 fails the job. Every script is called with
  `-Repository $GITHUB_REPOSITORY` (a fork reconciles itself); every `pwsh` command line
  is echoed before it runs; concurrency group `governance-apply` (plan: `governance-plan-<PR>`),
  never cancelled mid-pass.
- **`branch-prune.yml`** — `workflow_dispatch` only (`apply` default false, `keep`,
  `max_delete` default 200), `permissions: contents: write` + `pull-requests: read`,
  `GITHUB_TOKEN`: prints the command, runs `prune-branches.ps1 -Repository
  $GITHUB_REPOSITORY -Json -MaxDelete <n>`, renders the table and the report's `nextStep`
  into the job summary, uploads the `branch-prune-report` artifact (90 days), then
  re-raises the script's exit code.

```powershell
pwsh scripts/github/apply-rulesets.ps1                 # dry run
pwsh scripts/github/apply-repo-settings.ps1            # dry run
pwsh scripts/github/apply-labels.ps1 -Json             # one object
pwsh scripts/github/apply-milestones.ps1
pwsh scripts/github/prune-branches.ps1 -Json > branch-prune-report.json
pwsh scripts/github/close-duplicate-issues.ps1         # classification table, changes nothing
gh workflow run governance-apply.yml -f apply=true -f scope=all   # the apply path, from main
gh workflow run branch-prune.yml                       # plan; then -f apply=true -f keep=<a,b> [-f max_delete=<n>]
```
