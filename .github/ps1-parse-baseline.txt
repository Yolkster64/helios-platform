# .ps1 files with pre-existing parse errors (real Parser API, Linux), kept as an
# explicit baseline so the ci-validation syntax gate can be strict for everything
# else. Repairing a file? Delete its line. Adding new broken files is not allowed —
# the gate fails on any file with errors that is not listed here.
