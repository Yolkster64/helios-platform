# Yolkster64 GitHub Profile Publication Plan

The public profile repository is `Yolkster64/Yolkster64`. Its root `README.md` should be sourced from `docs/profile/YOLKSTER64-PROFILE-README.md` after the two canonical repository names resolve live.

## Publication checks

- owner is `Yolkster64`;
- repository name exactly matches owner;
- visibility is public;
- default branch is `main`;
- root `README.md` is non-empty;
- profile contains no tenant IDs, private URLs, credentials, machine identifiers, legal/health data, or internal evidence;
- links resolve to `Yolkster64/helios-control` and `Yolkster64/helios-gui` after cutover;
- profile repo has no product source, deployment workflow, package, or secret environment;
- canonical repositories are pinned manually after publication.

Creating the profile repository before the final target names exist is permitted only when the README marks those links as planned rather than falsely active.
