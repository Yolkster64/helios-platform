<!--
    HELIOS type ramp (GUI_UPGRADE_PLAN.md P1): the Material-3-style scale from
    src/gui/MonadoBlade.GUI/Styles/Typography.xaml ported onto the Fluent ramp as
    TextBlock styles. Mapping (source scale -> Fluent size):
        Display  (Display Small 36/44)   -> 40/52  (Fluent Title Large)
        Headline (Headline Medium 28/36) -> 28/36  (Fluent Title)
        Title    (Title Large 22/28)     -> 20/28  (Fluent Subtitle)
        Body     (Body Medium 14/20)     -> 14/20  (Fluent Body)
        Label    (Label Medium 12/16)    -> 12/16  (Fluent Caption, SemiBold)
    LetterSpacing (px) from the source becomes CharacterSpacing (1/1000 em).
    Foregrounds consume ONLY semantic tokens from Themes/Tokens.xaml via
    {ThemeResource} so they re-resolve on theme/pack changes; no literal colors here.
    Styles are self-contained (no BasedOn into the WinUI ramp) so parse-level
    verification on Linux stays honest.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Display: page hero / large KPI numerals -->
    <Style x:Key="HeliosDisplayTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display,Segoe UI" />
        <Setter Property="FontSize" Value="40" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="52" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Headline: page titles (the AI Hub header) -->
    <Style x:Key="HeliosHeadlineTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display,Segoe UI" />
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="36" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Title: card and section headings -->
    <Style x:Key="HeliosTitleTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display,Segoe UI" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="28" />
        <Setter Property="CharacterSpacing" Value="8" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Body: default reading text -->
    <Style x:Key="HeliosBodyTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text,Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="CharacterSpacing" Value="18" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Body strong: emphasized inline text (row titles) -->
    <Style x:Key="HeliosBodyStrongTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text,Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="20" />
        <Setter Property="CharacterSpacing" Value="7" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Caption: secondary metadata lines -->
    <Style x:Key="HeliosCaptionTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text,Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="LineHeight" Value="16" />
        <Setter Property="CharacterSpacing" Value="33" />
        <Setter Property="Foreground" Value="{ThemeResource TextSecondaryBrush}" />
    </Style>

    <!-- Label: badges, column headers, button-adjacent text -->
    <Style x:Key="HeliosLabelTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Text,Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="LineHeight" Value="16" />
        <Setter Property="CharacterSpacing" Value="42" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

    <!-- Monospace: code, logs, request/response payloads -->
    <Style x:Key="HeliosMonospaceTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Cascadia Code,Consolas,Courier New" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="LineHeight" Value="18" />
        <Setter Property="Foreground" Value="{ThemeResource TextPrimaryBrush}" />
    </Style>

</ResourceDictionary>
